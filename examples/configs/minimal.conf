# aria2-rust Minimal Configuration Template
# 
# Minimal configuration with only essential options.
# Suitable for quick setup or testing.

# Save directory
dir=~/downloads

# Continue partial downloads
continue=true

# Connection settings
max-connection-per-server=4
split=4

# That's it! Keep it simple.
